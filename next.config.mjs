/** @type {import('next').NextConfig} */
// const nextConfig = {};
const nextConfig = {
   experimental: {
     serverComponentsExternalPackages: ['@acme/ui'],
   },
 }

export default nextConfig;


 
